apiVersion: v1
kind: Service
metadata:
  name: web-app3
  namespace: devops
spec:
  selector:
    app: web-app3
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: app3
    protocol: TCP
    port: 80
    targetPort: 80
