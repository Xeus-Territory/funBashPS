apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-host
  namespace: devops
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: web-server
              port:
                number: 80
