apiVersion: v1
kind: Service
metadata:
  name: web-app1
  namespace: devops
spec:
  selector:
    app: web-app1
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: app1
    protocol: TCP
    port: 80
    targetPort: 80
