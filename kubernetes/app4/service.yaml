apiVersion: v1
kind: Service
metadata:
  name: web-app4
  namespace: devops
spec:
  selector:
    app: web-app4
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: app4
    protocol: TCP
    port: 80
    targetPort: 80
