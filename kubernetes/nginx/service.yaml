apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: devops
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  sessionAffinity: None
  type: ClusterIP
